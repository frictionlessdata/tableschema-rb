require 'spec_helper'

describe JsonTableSchema do
  it 'has a version number' do
    expect(JsonTableSchema::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
